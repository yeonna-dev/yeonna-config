{
  "name": "yeonna-config",
  "version": "0.0.1",
  "description": "Yeonna config module",
  "author": "osairis100@gmail.com",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -r dist && tsc",
    "build:watch": "tsc --watch",
    "dev": "concurrently -n Compile,Test 'npm run build:watch' 'npm run test:watch'",
    "test": "jest --verbose",
    "test:watch": "jest --watch --detectOpenHandles"
  },
  "dependencies": {
    "firebase": "^9.6.6"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^15.12.2",
    "concurrently": "^7.0.0",
    "dotenv": "^16.0.0",
    "jest": "^27.5.0",
    "nodemon": "^2.0.7",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.4.0",
    "typescript": "^4.3.2"
  }
}